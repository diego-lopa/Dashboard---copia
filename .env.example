# ==============================================================================
# VARIABLES DE ENTORNO DEL SISTEMA IOT / LORAWAN
# ==============================================================================

# Entorno
NODE_ENV=production
LOG_LEVEL=info

# PostgreSQL & TimescaleDB
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=iot
POSTGRES_USER=iot
POSTGRES_PASSWORD=changeme_db_password
DATABASE_URL=postgresql://iot:changeme_db_password@postgres:5432/iot?schema=public

# Redis & Colas BullMQ
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379

# Mosquitto MQTT Broker
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_URL=mqtt://mosquitto:1883
MQTT_USERNAME=iot
MQTT_PASSWORD=changeme
MQTT_TOPIC_SUBSCRIPTION=application/+/device/+/event/up

# Seguridad & Autenticación API
JWT_SECRET=super_secret_jwt_key_32_characters_minimum_entropy_random_hex
JWT_EXPIRES_IN=24h
API_KEY_INGEST=secret_ingest_key_for_http_gateways_123456

# Offline: minutos sin telemetría antes de marcar una sonda como offline
# (debe superar la cadencia de emisión; sondas cada 30 min → 90)
#OFFLINE_THRESHOLD_MINUTES=90

# Puertos y Red
API_PORT=8000
API_PUBLIC_URL=http://localhost:8000
FRONTEND_PORT=3000
VITE_API_URL=http://localhost:8000/api/v1

# Simulador automático (servicio `simulator` en docker-compose.yml).
# 30 min como las sondas físicas; primera ráfaga inmediata al arrancar.
# Bajar SIM_INTERVAL_MS (ms) para acelerar el tiempo simulado.
#SIM_SENSOR_COUNT=5
#SIM_INTERVAL_MS=1800000
#SIM_NEUTRON_MODE=true
#SIM_ANOMALIES=true
#SIM_BACKFILL=10

# ChirpStack
CHIRPSTACK_SERVER=chirpstack:8080
CHIRPSTACK_API_SECRET=default_jwt_secret_for_chirpstack_change_in_production
CHIRPSTACK_APPLICATION_ID=1

# Notificaciones y Alertas (Opcionales)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=alerts@example.com
SMTP_PASSWORD=changeme_smtp
SMTP_FROM="CORNEA IoT Platform <alerts@example.com>"
ALERT_WEBHOOK_URL=https://webhook.site/demo-alerts
